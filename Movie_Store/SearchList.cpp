#include <iostream>
#include <string>
#include <fstream>
#include "SearchList.h"

using namespace std;

void SearchList::expandArray(stringAndPtr * arr, stringAndPtr newtag)
{
    // expand array by creating copy and adding newtag to new slot
}
